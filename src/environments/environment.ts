// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyAqQGvcTIUTQzrd2esuTswwNT3JYcT0lcA",
    authDomain: "whatscore-d96e3.firebaseapp.com",
    databaseURL: "https://whatscore-d96e3.firebaseio.com",
    projectId: "whatscore-d96e3",
    storageBucket: "whatscore-d96e3.appspot.com",
    messagingSenderId: "514458289149",
    appId: "1:514458289149:web:121571852990e126"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
